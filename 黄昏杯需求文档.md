---

# 黄昏杯 (Twilight Cup) — 需求文档 v3.0

---

## 一、项目概述

**黄昏杯** 是一场《人类一败涂地》(Human: Fall Flat) 的 1v1 速通比赛。双方选手各自在单人游戏中独立挑战相同的项目，通过游戏内的 BepInEx 插件与外部系统通信。系统由 **一个中心服务端** 与 **三类客户端**（选手端、裁判端、导播端）构成，所有通信均由服务端中转。

选手端与裁判端通过 **同一聊天系统** 进行文字交流，聊天系统同时承担 **命令交互**（如准备就绪、掷骰、独立计时器等），所有命令以 `!` 开头。

---

## 二、系统架构

```
                    ┌──────────────┐
                    │   服务端      │
                    │  (Server)    │
                    └──┬───┬───┬──┘
          ┌────────────┘   │   └────────────┐
          │                │                │
     ┌────▼────┐     ┌────▼────┐     ┌─────▼─────┐
     │ 选手A端  │     │ 裁判端   │     │  导播端    │
     │(BepInEx) │     │(桌面应用) │     │(Web前端)  │
     └─────────┘     └──────────┘     └───────────┘
          │
     ┌────▼────┐
     │ 选手B端  │
     │(BepInEx) │
     └─────────┘
```

- **服务端**：中转所有消息，管理比赛会话，持久化数据，支持多场比赛并发且彼此严格隔离。
- **选手端**：以 BepInEx 插件形式内嵌于选手游戏客户端，包含聊天与计时器两个子模块。
- **裁判端**：桌面应用程序，裁判通过它管理比赛全流程。
- **导播端**：Web 前端，由 OBS 浏览器源捕获推流，导播手动切换不同展示页面。

---

## 三、核心概念定义

### 3.1 图池 (Map Pool)

图池是一场比赛所有可选项目的集合，结构如下：

```
图池
├── 类别 (Category)：ML / IL / CP / …（仅用于展示分组，无程序逻辑）
│   ├── 选图 (Pick)：ML1
│   ├── 选图 (Pick)：ML2
│   └── ...
├── 类别：IL
│   ├── 选图：IL1
│   └── ...
└── ...
```

每个 **选图** 即为一个 **项目**，一回合对应一个选图。比赛全程以"选图"为基本单位进行展示与操作。

### 3.2 选图属性

| 字段 | 说明 |
|------|------|
| 编号 | 如 `ML1`、`IL3`，在图池内唯一 |
| 项目名称 | 展示用 |
| 项目类型 | 多关 / 单关，可附加词条（如"单关 + 全存档点"） |
| 重试次数 | 仅当项目类型为单关时有效 |
| 关卡合集配置 | 定义本选图所包含的关卡列表及其顺序，由关卡合集插件定义格式，服务端原样存储并下发 |

### 3.3 项目类型

#### 多关项目

- 由若干不同关卡按顺序组成合集。
- 选手依次通关，总成绩 = 所有关卡用时之和。
- **中途退出任何一关 → 本回合立即弃权**。
- 无暂停、无重试某个关卡的机会。

#### 单关项目

- 同一关卡重复 N 次（N = 所选图的重试次数），由关卡合集插件自动编排。
- 选手每次尝试独立计时，可随时通过控制台命令 **跳过** 当前尝试，跳过者本次用时记作 **N/A**。
- 退出合集时：
  - ≥1 次有效成绩 → 回合正常结束，最终成绩确定。
  - 0 次有效成绩 → 本回合 **弃权**。
- 未完成的剩余尝试次数自动全部标记为 N/A。

### 3.4 成绩计算规则

| 项目类型 | 计算方式 | 备注 |
|----------|----------|------|
| 多关项目 | 所有关卡用时之和 | — |
| 单关项目 | **最快时间** 或 **平均时间** | 由服务端创建比赛时指定，二选一 |

### 3.5 平局与重赛

- 双方最终成绩完全相同时，本回合 **重赛**。
- 重赛由裁判在裁判端手动触发：重新下发同一关卡合集，双方重新开始。
- 上次回合数据 **不计入比分**，但 **保留在日志中** 供后续查看。
- 重赛无次数限制。

---

## 四、账号与会话系统

### 4.1 账号

- 自定义账号体系。
- 账号类型：`选手` | `裁判` | `导播` | `管理员`（赛事方）。
- 账号由管理员在服务端预创建并分发。

### 4.2 比赛会话

- 由管理员在服务端手动创建。
- 创建时指定：比赛名称、赛制（BO 数）、单关成绩计算方式（最快/平均）、完整图池、指派双方选手 / 裁判 / 导播的账号、**开始倒计时延迟秒数**（默认 5 秒）。
- 各客户端连接时选择加入已分配的会话。
- 服务端可同时承载多场比赛会话，彼此完全隔离。

---

## 五、聊天系统与命令

### 5.1 聊天范围

- 选手A、选手B、裁判三方群聊。
- 所有人可见所有消息（含系统消息）。
- 导播端只读围观，显示所有聊天内容及系统消息。
- 聊天记录持续累计，每回合不清空，计入独立日志。

### 5.2 命令系统

所有特殊命令以 `!` 开头，由服务端解析并执行。

### 5.3 通用命令

| 命令 | 可用角色 | 功能 |
|------|----------|------|
| `!ready` | 选手 | **切换**准备状态。未准备→已准备；已准备→未准备。 |
| `!roll` | 所有人 | 系统返回一个 1–100 的随机整数，以系统消息形式广播。 |

### 5.4 裁判专属命令

| 命令 | 功能 |
|------|------|
| `!counter [seconds]` | 启动一个独立倒计时器（秒数）。若已有激活的计时器，则报错提示需先 `!counter reset`。 |
| `!counter reset` | 停止当前激活的计时器。 |

#### `!counter` 提示规则

- 计时中，每剩余整分钟时（如 3:00、2:00、1:00）通过系统消息提示。
- 最后不足 1 分钟时：在 **30 秒、20 秒、10 秒**，以及最后 **5 秒逐秒**（5、4、3、2、1）提示。
- 计时归零时发送系统消息提示。
- `!counter` 为裁判独立辅助工具，与比赛流程无程序关联。
- 所有计时器系统消息同步给导播端。

### 5.5 系统消息

以下事件均以系统消息形式在聊天中广播，并同步导播端：

| 事件 | 消息示例 |
|------|----------|
| 裁判标记准备阶段开始 | `[系统] 准备阶段已开始，选手请输入 !ready 准备` |
| 选手切换 Ready | `[系统] 选手A 已准备就绪` / `[系统] 选手A 已取消准备` |
| 自动开始倒计时 | `[系统] 双方已就绪，比赛将在 5 秒后自动开始…`（逐秒提示） |
| 手动开始倒计时 | `[系统] 裁判已发起开始，比赛将在 5 秒后开始…`（逐秒提示） |
| 倒计时被取消 | `[系统] 自动开始倒计时已取消` |
| 回合开始 | `[系统] 回合开始！当前项目：ML1 - xxx` |
| 计时器提示 | `[系统] 倒计时剩余 2:00` 等 |
| `!roll` 结果 | `[系统] 选手A 掷出了 42（1-100）` |
| 命令错误 | `[系统] 当前已有激活的计时器，请先使用 !counter reset` |

---

## 六、比赛生命周期

### 6.1 回合准备阶段

1. 裁判在裁判端界面点击按钮，**标记进入回合准备阶段**。
   - 聊天系统广播系统消息通知。
2. 选手在聊天中输入 `!ready` 切换准备状态。
   - 裁判端实时显示双方准备状态。
   - 每次状态变更广播系统消息。
3. 双方均准备就绪后，**自动开始倒计时**（使用比赛配置的延迟秒数，默认 5 秒）：
   - 聊天中以系统消息逐秒提示倒计时。
   - 倒计时期间若有选手取消准备（`!ready` 切换），倒计时 **立即中断**，广播取消通知。
   - 倒计时归零 → 回合自动开始。
4. 裁判也可在准备阶段 **手动点击开始**：
   - 无论双方是否就绪，同样触发倒计时（使用同一延迟配置）。
   - 手动开始的倒计时 **不可被选手取消**，倒计时归零后强制开始。

### 6.2 回合开始

- 倒计时归零时：
  - 服务端将本回合所选图的 **关卡合集配置** 下发给双方选手端。
  - 双方游戏自动加载关卡合集并开始计时。
  - 聊天系统广播回合开始信息。

### 6.3 回合进行中

- 选手各自在单人游戏中独立完成项目。
- 裁判与选手三方可继续文字聊天。
- 裁判端可实时查看双方状态：游戏中 / 已完成 / 已弃权、当前关卡进度、已完成关卡用时。
- 导播端在每关完成时收到计时数据更新。

### 6.4 回合结束

- 双方均完成项目并发送"项目完成"信号后，导播端生成本回合统计信息，导播可手动切换至回合结算页面。
- 裁判根据双方成绩 **手动判定本回合胜负**（选手A胜 / 选手B胜 / 平局→触发重赛）。
- 系统自动累计比分。

### 6.5 比赛结束

- 当任一方比分达到设定阈值时（如 BO9 中率先达到 5 分），系统 **自动判定比赛结束**。
- 导播端可切换至结算画面，展示最终结果。

### 6.6 比赛计时

- 整个比赛无强制性计时器，一切行为由裁判按照赛事规定时间手动推进。
- 单回合超时由裁判人工判断与处理。

---

## 七、回合详细流程

```
选手A/B          裁判端            服务端            导播端
  │                │                 │                 │
  │← [系统] 准备阶段已开始 ─────────┤                 │
  │                │                 │                 │
  │─ !ready ──────→├────────────────→│                 │
  │← [系统] 选手A 已准备就绪 ────────┤                 │
  │                │                 │                 │
  │─ !ready ──────→│ (选手B)        │                 │
  │← [系统] 选手B 已准备就绪 ────────┤                 │
  │                │                 │                 │
  │← [系统] 5秒后自动开始 ──────────┤                 │
  │← [系统] 4… ────────────────────┤                 │
  │← [系统] 3… ────────────────────┤                 │
  │← [系统] 2… ────────────────────┤                 │
  │← [系统] 1… ────────────────────┤                 │
  │← 开始指令+合集配置 ─────────────┤                 │
  │← [系统] 回合开始！当前项目：ML1 │                 │
  │                │                 │                 │
  │═══ 关卡1 ════ │                 │                 │
  │── 关卡1完成 ────────────────────→├────────────────→│
  │   (总时长+单关时长)              │                 │
  │═══ 关卡2 ════ │                 │                 │
  │── 关卡2完成 ────────────────────→├────────────────→│
  │   (...)       │                 │                 │
  │═══ 全部完成 ═══                  │                 │
  │── 项目完成 ──────────────────────→├────────────────→│
  │                │                 │   (生成统计)     │
  │                ├─ 判定胜负 ──────→├────────────────→│
  │                │                 │                 │
```

- 单关项目：每次尝试完成（或跳过）即推送该次单关时间，不推送总时长。
- 多关项目：每关完成推送"当前总时长 + 本关单关时长"。

---

## 八、各端功能详述

### 8.1 服务端

| 功能 | 说明 |
|------|------|
| 多会话管理 | 同时承载多场比赛，会话间严格隔离 |
| 消息路由 | 转发各端之间的所有消息 |
| 命令解析 | 解析并执行 `!` 开头命令，返回系统消息 |
| 账户管理 | 创建/管理账户，分配角色 |
| 比赛创建 | 创建会话，配置赛制、图池、延迟秒数、指派人员 |
| 准备阶段管理 | 追踪双方准备状态，触发自动/手动开始倒计时 |
| 数据持久化 | 存储比赛日志、回合数据、聊天记录 |
| 合集配置下发 | 回合开始前将关卡合集配置原样下发选手端 |
| 自动判终 | 比分达阈值时自动判定比赛结束 |

### 8.2 选手端 (BepInEx 插件)

#### 聊天模块

- 与裁判端及对方选手三方群聊。
- 支持 `!` 命令输入，结果以系统消息返回。
- 消息通过服务端中转。

#### 计时器模块

- 完全在本地运行，基于关卡加载/卸载事件自动计时。
- **每关完成时**上报数据（非实时推送）：
  - 多关项目：当前总时长 + 本关单关时长。
  - 单关项目：本次尝试单关时间。
- 项目全部完成时发送"项目完成"信号。
- 退出关卡时自动发送弃权信号。
- 重连后同步本回合全部计时数据，补全断连期间缺失的上报。

#### 准备状态

- 通过聊天命令 `!ready` 切换。
- 准备状态下锁定关卡开始。
- 裁判手动开始倒计时期间无法取消准备。

#### 接收数据

- 开始指令（含关卡合集配置）。
- 对手计时数据（可实时控制显示/隐藏）。
- 裁判聊天消息。
- 回合信息。
- 系统消息。

### 8.3 裁判端（桌面应用）

| 功能分类 | 具体功能 |
|----------|----------|
| 比赛管理 | 加入指定会话 |
| 准备阶段 | 点击按钮标记进入回合准备阶段（触发系统消息），标记下一回合选图 |
| 状态监控 | 双方准备状态、比赛状态（游戏中/已完成/已弃权）、当前关卡进度、已完成关卡用时 |
| 回合控制 | 从图池中选择选图、手动点击开始发起回合（触发倒计时）、触发当前回合重赛 |
| 胜负判定 | 手动判定每回合胜负（A胜/B胜/平局），支持事后修改判定，修改实时同步导播端 |
| 数据查看 | 每回合双方详细成绩：单关项目每次尝试时间 + 赛制最终成绩（最快/平均），多关项目总时间 + 每关时间明细 |
| 强制操作 | 可在未全员就绪时强制开始；一方弃权后手动通知另一方 |
| 聊天 | 与双方选手三方群聊，可使用 `!roll`、`!counter` 命令 |
| 日志 | 维护整场比赛日志，记录初始信息 + 实时更新，含完整回合数据 |

#### 约束

- 裁判无权中途强制终止选手回合。
- 裁判无法临时修改图池。

### 8.4 导播端（Web 前端，本期暂缓实现）

| 页面 | 展示内容 |
|------|----------|
| 图池展示 | 本场比赛完整图池、类别分组 |
| 比赛进行中 | 双方实时状态、已完成关卡用时、最新计时数据、比分、当前项目信息、聊天及系统消息 |
| 回合结算 | 多关项目：总时间 + 每关时间明细；单关项目：每次尝试时间明细；胜负结果 |
| 比赛结算 | 最终比分、胜负、比赛总结 |
| 聊天 | 只读显示三方聊天内容及系统消息（页面位置由前端设计决定） |

- 导播手动切换页面。
- 数据随选手每关完成而更新，无实时逐帧推送。
- 双方均完成项目后，导播端生成本回合统计信息。

---

## 九、数据流汇总

### 9.1 选手端 → 服务端

- 准备状态变更（`!ready` 切换）
- 聊天消息（含命令：`!ready`、`!roll` 等）
- 计时数据（每关完成：总时长 + 单关时长 / 单关项目：每次尝试时间）
- 项目完成信号
- 弃权信号（退出关卡时）
- 心跳/连接状态

### 9.2 服务端 → 选手端

- 关卡合集配置（回合开始前）
- 开始指令
- 对手计时数据
- 聊天消息（含所有其他角色的消息）
- 系统消息（命令响应、倒计时提示、回合信息等）

### 9.3 裁判端 → 服务端

- 准备阶段标记
- 选定选图 + 开始指令（手动开始）
- 每回合胜负判定（支持修改）
- 重赛指令
- 聊天消息（含命令：`!roll`、`!counter`、`!counter reset`）
- 比赛元信息

### 9.4 服务端 → 裁判端

- 选手准备状态
- 选手计时数据
- 选手比赛状态（游戏中/已完成/已弃权）
- 选手当前关卡进度
- 聊天消息（含所有角色的消息）
- 系统消息
- 比分状态
- 自动开始倒计时触发（双方就绪时）

### 9.5 服务端 → 导播端

- 比赛基本信息（选手、赛制、图池）
- 每关完成时的计时数据
- 回合信息（当前选图、回合数）
- 回合结果与详细成绩
- 累计比分
- 比赛状态（准备中 / 倒计时中 / 进行中 / 回合结束 / 比赛结束）
- 聊天内容及系统消息（只读）
- 胜负判定（含修改后的更新）
- `!counter` 计时器状态及提示

---

## 十、异常处理

### 10.1 选手断连

| 场景 | 处理方式 |
|------|----------|
| 自动重连成功 | 选手端同步本回合全部计时数据，补全断连期间缺失的上报，比赛继续 |
| 重连失败，游戏仍可正常进行 | 选手继续比赛，完成项目后手动提交计时数据，赛后与技术人员排查网络 |
| 客户端崩溃或状态不可判断 | 裁判决定是否终止当前回合 → 通过聊天通知另一选手 → 裁判判定重赛或断连选手判负 |

### 10.2 弃权

| 场景 | 触发方式 | 处理 |
|------|----------|------|
| 多关项目中途退出 | 选手端自动发送弃权信号 | 本回合弃权 |
| 单关项目退出且 0 次有效成绩 | 选手端自动发送弃权信号 | 本回合弃权 |
| 单关项目退出且 ≥1 次有效成绩 | 正常回合结束 | 以已有有效成绩为准 |
| 一方弃权后 | 裁判手动通知另一方 | 另一方自行选择继续完成或直接结束 |

### 10.3 回合超时

- 无自动超时机制。
- 由裁判人工判断并处理。

### 10.4 提前起跑

- 准备状态下选手端锁定关卡开始，无法手动触发。
- 仅在倒计时归零、服务端发令后统一解锁。

### 10.5 倒计时中断

- 自动开始倒计时期间，任一选手取消准备 → 倒计时立即中断，系统消息广播。
- 手动开始倒计时不可被选手中断。

---

## 十一、日志与数据持久化

### 11.1 比赛日志

- 裁判端维护整场比赛日志。
- 内容包含：
  - 比赛初始信息（赛制、图池、选手信息、延迟配置等）
  - 每回合全程数据（选图、双方每次尝试/每关用时、最终成绩、胜负判定）
  - 重赛记录（保留原始数据，不计入比分）
  - 比赛最终结果
- 日志在比赛过程中实时更新。

### 11.2 聊天日志

- 聊天记录持续累计，每回合不清空。
- 包含所有用户消息和系统消息。
- 计入独立日志。

---

## 十二、分阶段实施计划

### 第一期（本期优先）

- 服务端核心功能（会话管理、消息路由、命令解析）
- 选手端 BepInEx 插件（聊天 + 计时器）
- 裁判端桌面应用（完整功能）
- 三端之间完整通信链路

### 第二期（后续实现）

- 导播端 Web 前端（多页面展示）
- 账号系统与管理后台
- 完善的可视化界面

---

## 十三、术语对照

| 术语 | 定义 |
|------|------|
| 图池 (Mappool) | 一场比赛中所有可选项目的集合 |
| 类别 (Category) | 图池中的分组，如 ML、IL、CP，仅用于展示 |
| 选图 (Pick) | 图池中的单个项目，对应一个回合 |
| 关卡合集 (Collection) | 若干关卡按顺序组成的列表，由插件定义格式 |
| 多关项目 (Multi Level) | 由不同关卡组成的合集，成绩为总用时之和 |
| 单关项目 (Single Level) | 同一关卡重复 N 次（含重试），成绩取最快或平均 |
| 跳过 (Skip) | 单关项目中放弃当前尝试，用时记为 N/A |
| 弃权 (Forfeit) | 多关项目中途退出，或单关项目无有效成绩时退出 |
| 重赛 (Rematch) | 平局时重新进行完全相同的回合 |
| 准备阶段 (Prep Phase) | 回合开始前，裁判标记后选手通过 `!ready` 准备的阶段 |
| 倒计时延迟 | 回合开始前的倒数秒数，默认 5 秒，可配置 |
| BO (Best Of) | 赛制，如 BO9 指先得 5 分者胜 |

---
